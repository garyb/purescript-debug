{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache && rimraf .psa-stash",
    "build": "jshint src && jscs src && pulp build --censor-lib --strict --stash",
    "test": "pulp test"
  },
  "devDependencies": {
    "jscs": "^2.8.0",
    "jshint": "^2.9.1",
    "pulp": "^8.0.0",
    "purescript-psa": "^0.3.6",
    "purescript": "^0.8.5",
    "rimraf": "^2.5.0"
  }
}
